# name: universal-ir
# suffix: ~  # add suffix to saved images
model: ff_model
# distortion: [motion-blurry,hazy,jpeg-compressed,low-light,noisy,raindrop,rainy,shadowed,snowy,uncompleted] # uncompleted means inpainting
# distortion: ~ # uncompleted means inpainting
# gpu_ids: [4]
# normalize_to_neg1_1: true
# save_lqgt: false

# sde:
#   max_sigma: 50
#   T: 100
#   schedule: cosine # linear, cosine
#   eps: 0.005
#   sampling_mode: posterior # sde or posterior
  
# degradation:
#   # for denoising
#   sigma: 25
#   noise_type: G # Gaussian noise: G

#   # for super-resolution
#   scale: 4

#### network structures
network_G:
  which_model_G: UNet
  setting:
    in_nc: 3
    out_nc: 3
    nf: 128
    ch_mult: [1, 2, 4, 4]
    context_dim: 512
    use_daclip_context: false
    # use_degra_context: true
    # use_image_context: true
    in_ch_scale: 1
    attn: false

#### path
path:
  root: ./
  # pretrain_model_G: pretrained/universal-ir.pth
  # pretrain_model_G: log/500000_G.pth
  # pretrain_model_G: log/universal-ir/models/lastest_EMA.pth
  # pretrain_model_G: experiments/universal-ir_251002-105026/models/200000_G.pth
  # pretrain_model_G: experiments/universal-ir_251003-154500/models/360000_G.pth
  # pretrain_model_G: experiments/universal-ir_251006-165812/models/600000_G.pth
  # pretrain_model_G: experiments/feedforward-ir_251009-182704/models/240000_G.pth
  pretrain_model_G: experiments/feedforward-ir_251015-195349/models/580000_G.pth
  # daclip: pretrained/daclip_ViT-B-32.pt
